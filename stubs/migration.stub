const { Schema } = require('guruorm');

class {{class}} {
  async up() {
    await Schema.create('{{table}}', (table) => {
      table.id();
      table.string('name');
      table.timestamps();
    });
  }

  async down() {
    await Schema.dropIfExists('{{table}}');
  }
}

module.exports = {{class}};
